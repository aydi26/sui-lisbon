# STATUS.md — per-task ledger, 2026-07-25

> Purpose: the honest state of every `docs/BUILD-PLAN.md` task after the **skeleton pass**. This file is regenerated by inspecting the tree, not by trusting agent reports.
> Read after: `docs/RECON.md`, `docs/GOLDEN-RULES.md`, `docs/CONVENTIONS.md`. Execution order still comes from `docs/BUILD-PLAN.md`; the entry document for the implementation run is `docs/ULTRACODE-BRIEF.md`.

## Headline

**The skeleton is complete and every aggregate verification is green.** Every file named by the specs exists, compiles, and carries a `docs/CONVENTIONS.md` contract banner. **No feature body is implemented yet** — Phases 1–5 are stubs with `TODO(Tx.y)` markers.

```
powershell -NoProfile -File scripts/verify-all.ps1     →  8 PASS · 0 FAIL · 0 SKIP
```

| Aggregate | Result | Measured |
|---|---|---|
| `move build` · `move test` | PASS | 37/37 tests, zero warnings |
| `keeper typecheck` · `build` · `test` | PASS | 44/44 tests in 5 files |
| `app build` | PASS | `tsc --noEmit && vite build`, 1029 modules |
| `gates` (g7 g4 g2 ids sdk purity transport todo) | PASS | 7 PASS · 0 FAIL · 0 SKIP |
| `verify-onchain` | PASS | 25 PASS · 0 FAIL · 0 WARN · 8 INFO (live testnet) |

**TODO census: 322 markers across 24 task ids in 100 files.** That census is the work-remaining backlog — see `docs/ULTRACODE-BRIEF.md`.

## Status vocabulary

| Status | Meaning |
|---|---|
| **DONE** | Implemented AND covered by a test or gate that I ran and saw pass. Safe to build on. |
| **PARTIAL** | Some functions genuinely implemented and tested; `TODO(Tx.y)` markers remain in the same file. |
| **STUB** | File exists, compiles, carries a banner. Bodies throw or are empty. **A green build is not coverage.** |
| **BLOCKED** | Cannot proceed without an action listed under Known blockers. |
| **NOT-STARTED** | No file exists yet. |

---

## Phase 0 — Scaffold + day-one gate

| id | Title | Status | Files that carry it | VERIFY | Note |
|---|---|---|---|---|---|
| T0.1 | Run day-one gate | **PARTIAL** | `docs/DAY-ONE-RESULTS.md`, `scripts/verify-onchain.mjs` | `node scripts/verify-onchain.mjs` | D1–D10 executed live; every UNKNOWN resolved or logged. **The two human-ops items (signet faucet drip, book seeder) were never started** — see blockers B1/B2. |
| T0.2 | Move scaffold | **DONE** | `move/Move.toml`, `move/Move.lock`, `move/sources/*.move` | `cd move && sui move build` | Package `aphotic`, `edition = "2024.beta"`. Both git deps resolve via their `Published.toml`. No `[environments]` block — see blocker B3. |
| T0.3 | Keeper scaffold | **DONE** | `keeper/package.json`, `tsconfig*.json`, `vitest.config.ts`, `src/config.ts`, `src/types.ts`, `src/sui/**`, `src/util/**` | `cd keeper && npm install && npm run build` | ESM, strict TS. `loadConfig(env)` is pure. `@mysten/sui 2.22.1` + `@mysten/hashi 0.6.0` pinned. `npm run dev` will not run until T2.10 fills `src/index.ts`. |
| T0.4 | App scaffold | **DONE** | `app/package.json`, `vite.config.ts`, `src/config.ts`, `src/App.tsx`, `src/routes.tsx`, `src/main.tsx`, `src/components/**`, `src/fixtures/**`, `src/lib/**`, `src/landing/**` | `cd app && npm run build` | React 19 + Vite 6. Four routes (`/` landing, `/deposit`, `/exit`, `/transparency`). Landing page ported with textures vendored locally. |
| T0.5 | Hashi adapter + deterministic MOCK | **DONE** | `keeper/src/hashi/{types,limiter,adapter,eventTypes,normalize,mock,index}.ts`, `keeper/test/{limiter.golden,limiter.consume,hashi.mock,limiter.cross}.test.ts` | `cd keeper && npm run test -- hashi.mock` | 13 mock tests + 21 limiter tests green. Logical-clock simulation: no `Date.now()`, no `Math.random()`, no I/O. **This is the load-bearing real code of the whole skeleton.** |
| T0.6 | Config wiring proof (G7) | **DONE** | `keeper/.env.example`, `app/.env.example`, `scripts/gates.{ps1,sh}` | `powershell -File scripts/gates.ps1 ids` | Canonical ids appear only in `keeper/src/config.ts`, `app/src/config.ts`, the two `.env.example` files and `move/Move.toml`. |

---

## Phase 1 — Move package (CUT-LINE CRITICAL)

| id | Title | Status | Files that carry it | VERIFY | Note |
|---|---|---|---|---|---|
| T1.1 | Vault object + share math | **STUB** | `move/sources/vault.move`, `move/tests/vault_tests.move` | `cd move && sui move build` | 20 `TODO(T1.1)`. ⚠ `vault_tests.move` holds **15 `#[test]` functions with EMPTY bodies** — they pass because they assert nothing. Green `sui move test` is NOT coverage here. `Vault<phantom B, phantom Q>` is generic over the asset pair (naming hBTC in `vault.move` would break the G7 gate). |
| T1.2 | `seal_approve` gate | **STUB** | `move/sources/vault.move` | `cd move && sui move test` | 14 `TODO(T1.2)`. Identity namespaced to vault id + version epoch; `rotate_keeper` increments it. |
| T1.3 | Gateway: register + composed exit | **PARTIAL** | `move/sources/gateway.move` | `cd move && sui move test --filter gateway` | 4 `TODO(T1.3)`. **Real and tested:** `assert_valid_exit_address`, `clears_hashi_minimum`, `hashi_withdrawal_min_sats() = 30_000`, `dust_floor_sats() = 546`. Stubbed: `register_exit_address`, `exit_to_bitcoin`. Only file allowed to contain `hashi::` (G7 gate confirms exactly one). |
| T1.4 | Gateway: reclaim + small-exit pool | **STUB** | `move/sources/gateway.move` | `cd move && sui move test --filter gateway` | 4 `TODO(T1.4)`. ⚠ `reclaim_stalled_exit` is **DEPOSITOR-ONLY** and any pooled flush must assert `who == ctx.sender()` (E-M8). |
| T1.5 | Router: maker + IOC | **PARTIAL** | `move/sources/router.move`, `move/tests/router_tests.move` | `cd move && sui move test --filter router` | 17 `TODO(T1.5)`. **Real and tested:** `assert_order_granularity(price, quantity, tick, lot, min_size)` + the three defaults (config-overridable by design). Stubbed: the DeepBook entrypoints. ⚠ `place_post_only_limit_order` / `best_bid_price` / `best_ask_price` are **not deployed** in v20 (E-M6). |
| T1.6 | Gateway unit tests | **STUB** | `move/tests/gateway_tests.move`, `move/tests/mock_hashi.move` | `cd move && sui move test --filter gateway` | 16 `TODO(T1.6)`. `mock_hashi.move` is **real** — it replicates `request_withdrawal`/`cancel_withdrawal` with all five upstream asserts. It verifies gateway's *logic*, not gateway's own bodies (Move has no DI). |

---

## Phase 2 — Keeper (CUT-LINE CRITICAL)

| id | Title | Status | Files that carry it | VERIFY | Note |
|---|---|---|---|---|---|
| T2.1 | Real Hashi adapter | **STUB** | `keeper/src/hashi/real.ts` | `cd keeper && npm run test -- hashi.real` | 16 `TODO(T2.1)`; every method throws `NotImplementedError`. ⚠ Most methods `docs/KEEPER.md` §2 describes **do not exist** in `@mysten/hashi@0.6.0` (E-K8) — build them from `HashiClient` + raw `moveCall`s + event polling. The guardian needs **HTTP/2** (E-K9). No test file exists yet. |
| T2.2 | Event watcher | **STUB** | `keeper/src/hashi/watcher.ts` | `cd keeper && npm run test -- watcher` | 4 `TODO(T2.2)`. Normalization already exists in `hashi/normalize.ts` (DONE) — reuse it, do not rewrite the `WithdrawalSigned` join. No test file exists yet. |
| T2.3 | `confirm_deposit` crank | **STUB** | `keeper/src/execution/crank.ts` | `cd keeper && npm run test -- crank` | 8 `TODO(T2.3)`. Permissionless, idempotent, respects the 600 000 ms delay. ⚠ `confirm_deposit` is `entry` — a PTB command, never a Move `moveCall` inside `gateway.move` (E-M9). This is the live on-camera demo beat. No test file exists yet. |
| T2.4 | Sponsored deposit sweep | **STUB** | `keeper/src/execution/sweep.ts` | `cd keeper && npm run test -- sweep` | 7 `TODO(T2.4)`. No test file exists yet. |
| T2.5 | Withdrawal tracker | **STUB** | `keeper/src/execution/exit.ts`, `keeper/src/execution/reclaim.ts` | `cd keeper && npm run test -- exit` | 14 `TODO(T2.5)`. ⚠ `reclaim.ts` must be an **unsigned PTB builder only** — the keeper can never sign a reclaim (E-K7). No test file exists yet. |
| T2.6 | Strategy: Seal + deterministic evaluate/route | **STUB** | `keeper/src/strategy/{params,serialize,evaluate}.ts`, `keeper/src/privacy/{seal,session,rotation}.ts` | `cd keeper && npm run test -- strategy` | 37 `TODO(T2.6)` — the single largest task. `strategy/index.ts` barrel is DONE. `evaluate()`/`route()` must stay pure (the `purity` gate enforces no `Date.now()`/`Math.random()`). No test file exists yet. |
| T2.7 | Routing: L2 book + maker/IOC split | **STUB** | `keeper/src/routing/{book,deepbook,route,taker}.ts`, `keeper/src/execution/trade.ts` | `cd keeper && npm run test -- routing` | 32 `TODO(T2.7)`. `routing/index.ts` barrel is DONE. ⚠ Read via `get_level2_range`, never `mid_price` (aborts `EEmptyOrderbook` on the empty book) — E-K6/E-M7. No test file exists yet. |
| T2.8 | Oracle divergence breaker | **STUB** | `keeper/src/oracle/{pyth,twap,divergence,index}.ts` | `cd keeper && npm run test -- oracle` | 22 `TODO(T2.8)`. Pyth **Beta** feed `0xf9c0172b…`; match `attributes.symbol === "Crypto.BTC/USD"` exactly. No test file exists yet. |
| T2.9 | Storage: Walrus put/get | **STUB** | `keeper/src/storage/{walrus,renew}.ts` | `cd keeper && npm run test -- storage` | 18 `TODO(T2.9)`. `WALRUS_EPOCHS` explicit (`< 2` already throws in `config.ts`). ⚠ Fresh blobs return `certifiedEpoch: null` — allow a grace window (E-M11). No test file exists yet. |
| T2.10 | End-to-end run loop (mock) | **STUB** | `keeper/src/index.ts`, `keeper/src/execution/trade.ts` | `cd keeper && npm run test -- e2e.mock` | 7 `TODO(T2.10)`. `index.ts` is PARTIAL (CLI shell only). **`npm run dev` cannot run until this lands.** ⚠ **The cut-line VERIFY command `npm run test -- e2e.mock` currently matches no test file** — the e2e test must be written as part of this task. |

---

## Phase 3 — App (CUT-LINE CRITICAL)

| id | Title | Status | Files that carry it | VERIFY | Note |
|---|---|---|---|---|---|
| T3.1 | Deposit screen | **STUB** | `app/src/screens/deposit/DepositScreen.tsx`, `app/src/components/QrCode.tsx`, `app/src/hashi/index.ts` | `cd app && npm run build` | 5 `TODO(T3.1)`. Supporting components `AddressPill`, `LifecycleStepper` and `fixtures/deposit.ts` are DONE. ⚠ `generateDepositAddress` takes **four** args and a 32-byte `Uint8Array` (E-K10/E-A5). |
| T3.2 | Exit screen | **STUB** | `app/src/screens/exit/ExitScreen.tsx` | `cd app && npm run build` | 6 `TODO(T3.2)`. `PinningExplainer`, `explorer.ts`, `fixtures/exit.ts` are DONE; `lib/format.ts` is PARTIAL. ⚠ The reclaim button must be signed by the depositor's own zkLogin session (E-A3). |
| T3.3 | zkLogin + sponsored path | **STUB** | `app/src/session/zkLogin.ts`, `app/src/screens/deposit/DepositScreen.tsx` | manual: login → deposit with zero user SUI | 8 `TODO(T3.3)`. Sponsorship changes who *pays*, never who the *sender* is. |

---

## ================= CUT LINE =================

Cut-line VERIFY (all three must pass before any Phase 4+ work):

```
cd move   && sui move build && sui move test          # PASSING TODAY (37/37) — but T1.1–T1.6 bodies are stubs
cd keeper && npm run build && npm run test -- e2e.mock # BUILD passes; e2e.mock test DOES NOT EXIST yet (T2.10)
cd app    && npm run build                             # PASSING TODAY
```

⚠ **Two of these three are green only because the bodies are empty.** The cut line is *not* met. What actually gates it: T1.1, T1.3, T1.5 (Move bodies) → T2.3, T2.4, T2.5, T2.6, T2.7 → T2.10 → T3.1, T3.2.

## ============================================

---

## Phase 4 — Envelope + journal + verify (post-cut-line)

| id | Title | Status | Files that carry it | VERIFY | Note |
|---|---|---|---|---|---|
| T4.1 | Envelope: redemption buffer | **PARTIAL** | `move/sources/envelope.move`, `move/tests/envelope_tests.move` | `cd move && sui move test --filter envelope` | 13 `TODO(T4.1)`. **Real and tested (9 tests):** `project_capacity`, `project_capacity_secs`, `replay_consume`, `saturating_add/sub/mul`, `LimiterState`. This is the **G5 Move twin** of `keeper/src/hashi/limiter.ts`. Stubbed: `deployable_sats`, `check_action`. `envelope` is the intra-package **leaf** and takes primitives, never `&Vault` (Move forbids the cycle). |
| T4.2 | Journal: decision records | **STUB** | `keeper/src/journal/{schema,record}.ts`, `move/sources/journal.move`, `move/tests/journal_tests.move` | `cd keeper && npm run test -- journal` | 25 `TODO(T4.2)`. `journal::assert_monotonic_seq` is real and tested (2 tests). No keeper test file exists yet. |
| T4.3 | Verify: trustless limiter replay | **STUB** | `keeper/src/verify/{index,limiter,replay}.ts` | `cd keeper && npm run test -- verify` | 16 `TODO(T4.3)`. ⚠ `deriveLimiter` currently lives **inline in `keeper/test/limiter.cross.test.ts`**; when `src/verify/limiter.ts` lands, DELETE the local copy and import the real one or the G5 parity assertion silently drifts. |

---

## Phase 5 — Stretch (post-cut-line)

| id | Title | Status | Files that carry it | VERIFY | Note |
|---|---|---|---|---|---|
| T5.1 | Peg-flow signal | **STUB** | `keeper/src/strategy/pegflow.ts` | `cd keeper && npm run test -- strategy.pegflow` | 4 `TODO(T5.1)`. Signal public, response Seal-encrypted. |
| T5.2 | Transparency panel | **STUB** | `app/src/screens/transparency/TransparencyScreen.tsx`, `app/src/components/BridgeColumn.tsx` | `cd app && npm run build` | 5 `TODO(T5.2)`. `fixtures/{bridge,transparency}.ts`, `StaleBanner`, `TrustModelDisclosure`, `KeeperCapabilityBadge` are DONE. ⚠ **Do not write congestion copy** — the bucket is 100 BTC/day (E-A6). |
| T5.3 | Deposit-ticket TTO | **NOT-STARTED** | — | `cd move && sui move test --filter ticket` | No file exists. **Unblocked:** D6 resolved U4 = YES — `generateDepositAddress` accepts any 32-byte value including a synthetic object id. |

---

## Environment

| | |
|---|---|
| `sui` CLI | **1.76.0-6effb4523834** at `%LOCALAPPDATA%\sui\sui.exe`. ⚠ Not always on the shell `PATH` in agent shells — invoke as `& "$env:LOCALAPPDATA\sui\sui.exe"` or prepend to `PATH`. |
| `sui client active-env` | `testnet`, chain id `4c78adac` |
| node / npm | 24.13.0 / 11.6.2 |
| Active Sui address | `0x883ff25499d099a0e578a781acf03ff251647ca2430a2cef03257b080ea01125` (~1.86 SUI) |
| **Deployer / keeper address** | **`0xd41b0cd83fc1a497a5899eb686e2c7561e75e6d62db2270860d72542f63f333d`** (~2.00 SUI) |
| Secret handling | The private key lives **only** in the OS Sui keystore and `keeper/.env` (gitignored; `SUI_PRIVATE_KEY=` is currently **empty** — the user fills it with `sui keytool export --key-identity <alias>`). Never in `.env.example`, any doc, or a commit. |
| **RPC transport policy** | `https://fullnode.testnet.sui.io:443` serves **gRPC v2 only — JSON-RPC returns HTTP 404**. Default client is `SuiGrpcClient` from `@mysten/sui/grpc`, constructed in **exactly one place per package**: `keeper/src/sui/client.ts` and `app/src/lib/suiClient.ts`. The `transport` gate enforces this. `SuiClient` no longer exists in `@mysten/sui@2.22.1`; the JSON-RPC class is `SuiJsonRpcClient` and is for probes against the mirror `https://rpc-testnet.suiscan.xyz:443` only. |
| Guardian transport | `GET {guardian_url}/info` is **HTTP/2 only** (HTTP/1.1 returns 464). Use `node:http2`; Node's global `fetch`, `curl`, and the SDK's `fetchGuardianInfo` all fail. |
| Windows gotcha | Do **not** rewrite `.move` files with PowerShell `Set-Content -Encoding utf8` — PS 5.1 writes a UTF-8 BOM and the Move compiler rejects it (`E01001 Unexpected character: \u{feff}`). Use `[System.IO.File]::WriteAllText` with `New-Object System.Text.UTF8Encoding($false)`. |

---

## Known blockers

| # | Blocker | Severity | Owner | Detail |
|---|---|---|---|---|
| **B1** | **Signet faucet drip never started** | **HIGH — schedule** | build lead (human) | D7 is PARTIAL: both faucets return HTTP 200 but **no sats were ever requested**. Needs a signet wallet + a human captcha. Without hBTC inventory there is no deposit to pre-stage (G6) and no book to seed. **Start today.** |
| **B2** | **hBTC/DBUSDC book is empty on both sides; the scripted seeder account does not exist** | **HIGH — functional** | build lead (human) | `verify-onchain` confirms `get_level2_range` returns 0 levels and `mid_price` aborts `EEmptyOrderbook`. `treasury::mint` is `public(package)` so **we cannot mint hBTC ourselves**. NAV, the router, and the transparency panel all have nothing to read until a second account quotes. This is a **required component**, not a demo nicety. Depends on B1. |
| **B3** | `move/Move.toml` cannot declare `[environments]` | LOW — resolved by omission | — | `sui` 1.76.0 rejects `testnet = "4c78adac"`: *"Cannot override default environments … System environments: testnet, mainnet."* The block is deliberately absent and documented in the file header. No action needed. |
| **B4** | `move/tests/vault_tests.move` has 15 empty-body tests | MEDIUM — false confidence | T1.1/T1.2 implementer | They pass while asserting nothing. **Do not read `37/37 green` as vault coverage.** Fill them in the same run that fills `vault.move`. |
| **B5** | `npm run test -- e2e.mock` matches no test file | MEDIUM — cut-line | T2.10 implementer | The cut-line VERIFY command silently passes-by-vacuum today. The e2e mock test must be authored as part of T2.10. |
| **B6** | `deriveLimiter` is duplicated in a test file | MEDIUM — G5 drift | T4.3 implementer | The G5 parity assertion in `keeper/test/limiter.cross.test.ts` compares against a **local copy**. When `keeper/src/verify/limiter.ts` lands, delete the copy and import the real one. Carries a `TODO(T4.3)`. |
| **B7** | `keeper/src/hashi/real.ts` imports nothing yet | LOW — gate vacuity | T2.1 implementer | Nothing in the repo imports `@mysten/hashi`, so the `sdk` isolation gate currently passes **vacuously**. It only becomes load-bearing at T2.1. |
| **B8** | `move/Move.lock` records Windows backslash subdir paths | LOW — portability | whoever builds on CI | e.g. `subdir = 'crates\sui-framework\packages\move-stdlib'`. Machine-generated; may need regeneration on Linux/macOS. |
| **B9** | Lint suppressions must be removed | LOW — hygiene | each Move implementer | Every stub module carries `#[allow(unused_const, unused_field)]` with a "DELETE when the module status becomes DONE" comment. |
| **B10** | U1 genesis scalars: use the **live** values, not the old prior | LOW — was HIGH | — | Resolved by D4. `refill_rate = 115_740` sats/s, `max_bucket_capacity = 10_000_000_000` sats (100 BTC). The `1000` / `100_000_000` prior in older docs is wrong by ~100×. Some scaffold defaults may still carry the old prior — check `keeper/src/config.ts` and `.env.example` before `real` mode. |

### Docs that still need a correction pass (not blocking code)

- `docs/RECON.md` **R9 golden vectors #1 and #7** print `105_000`; the correct value is **`100_150`**. Both the Move and TS twins already encode `100_150` and cross-test green — only the doc is wrong.
- `docs/RECON.md` **R3** states the framework rev is `22f9fc97…`; the resolver actually pins `d50b7888…` because under the new package system the framework comes from the CLI's system environment. Harmless, but misleading.
- `docs/FACTS.md#events` still lists `utxo_pool::UtxoSpent`, which is **absent** from the verified R8 event inventory and is not modelled.
- `docs/BUILD-PLAN.md` canonical-identifier table still lists the **superseded v17** DeepBook package `0x22be4cad…` and the **stable-channel** Pyth feed `0xe62df6c8…`. Use v20 `0xd874d241…` and Beta `0xf9c0172b…`.
