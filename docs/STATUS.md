# STATUS.md — per-task ledger, 2026-07-25 (post-implementation pass)

> Purpose: the honest state of every `docs/BUILD-PLAN.md` task. **Regenerated by inspecting the tree and running every command below**, not by trusting an agent report. Every number in this file was observed, not quoted.
> Read after: `docs/RECON.md`, `docs/GOLDEN-RULES.md`, `docs/CONVENTIONS.md`, `docs/DEPLOYED.md`. Execution order comes from `docs/BUILD-PLAN.md`; the demo runbook is `docs/DEMO.md`.

## Headline

**The Move package is complete and published on testnet. The keeper is complete, CLI included. The app builds, ships three real screens, and now has its own test suite.** Two things are genuinely not done in code — the **exit-screen amount form** (`app/src/screens/exit/ExitScreen.tsx`, T3.2) and the **landing-page stats** (`app/src/landing/stats.js`, T5.2) — plus two things that are not code: the `ids` gate regression (**B11**) and the **empty order book** (**B2**).

Measured 2026-07-25, **16:26–16:36 local** (real output in the run log at the bottom). ⚠ **These counts are a moving snapshot.** Four agents were committing to disjoint paths during this pass; the keeper CLI (T2.10), the app test suite and `scripts/seed-book.*` all landed *while this file was being written*, and the keeper suite went 430 → 476 → **481** across three runs ten minutes apart. Re-run the commands rather than quoting this table. What is stable is the *shape*: everything green, one gate failing, two files with TODOs.

| Aggregate | Result | Measured |
|---|---|---|
| `cd move && sui move build` | **PASS** | exit 0, zero warnings |
| `cd move && sui move test` | **PASS** | `Total tests: 139; passed: 139; failed: 0` |
| `cd keeper && npm test` | **PASS** | `19 passed (19) files · 481 passed (481) tests` (16:36; was 476 at 16:26) |
| `cd keeper && npm run test -- e2e.mock` | **PASS** | `1 file · 13 tests` — **B5 closed** |
| `cd keeper && npm run typecheck` · `npm run build` | **PASS** | exit 0 |
| `cd app && npm test` | **PASS** | `6 passed (6) files · 84 passed (84) tests` |
| `cd app && npm run build` | **PASS** | `tsc --noEmit && vite build` |
| `node scripts/verify-onchain.mjs` | **PASS** | `28 PASS · 0 FAIL · 0 WARN · 7 INFO` (live testnet) |
| `powershell -File scripts/gates.ps1` | **FAIL (1 of 8)** | `6 PASS · 1 FAIL` — the `ids` gate, see **B11** |
| `powershell -File scripts/verify-all.ps1` | **FAIL (1 of 8)** | `7 PASS · 1 FAIL · 0 SKIP` — the single failure is the `gates` step above |

**TODO census: 4 markers across 2 task ids, in exactly two source files** (`app/src/screens/exit/ExitScreen.tsx` ×3, `app/src/landing/stats.js` ×1). Down from 322.

Move tests by module: `envelope_tests` 34 · `gateway_tests` 26 · `journal_tests` 12 · `router_tests` 25 · `vault_tests` 42.
Test totals at 16:36: **139 Move + 481 keeper + 84 app = 704**, plus 28 live on-chain assertions and 7 passing gates.

## Status vocabulary

| Status | Meaning |
|---|---|
| **DONE** | Implemented AND covered by a test or gate **that I ran and saw pass**. Safe to build on. |
| **PARTIAL** | Some functions genuinely implemented and tested; `TODO(Tx.y)` markers remain in the same file, or one named dependency is still a port. |
| **STUB** | File exists, compiles, carries a banner. Bodies throw or are empty. **A green build is not coverage.** |
| **BLOCKED** | Cannot proceed without an action listed under Known blockers. |
| **NOT-STARTED** | No file exists yet. |

---

## Phase 0 — Scaffold + day-one gate

| id | Title | Status | Files that carry it | VERIFY | Note |
|---|---|---|---|---|---|
| T0.1 | Run day-one gate | **PARTIAL** | `docs/DAY-ONE-RESULTS.md`, `scripts/verify-onchain.mjs`, `scripts/register-deposit.ps1` | `node scripts/verify-onchain.mjs` → `28 PASS · 0 FAIL` | D1–D10 executed live; every UNKNOWN resolved or logged. **Signet sats received** (B1 closed): tx `2275d890…deac9`, block 314710, vouts 238 + 248, 144 137 sats each. At the last check the tip was 314714 ⇒ **5 of the 6 required confirmations**; the UTXO has therefore **not been registered yet** and no hBTC exists. The book seeder (B2) was never started. |
| T0.2 | Move scaffold | **DONE** | `move/Move.toml`, `move/Move.lock`, `move/sources/*.move` | `cd move && sui move build` | Package `aphotic`, `edition = "2024.beta"`, zero warnings. Exactly two git deps; no `[environments]` (B3, resolved by omission). |
| T0.3 | Keeper scaffold | **DONE** | `keeper/package.json`, `tsconfig*.json`, `vitest.config.ts`, `src/config.ts`, `src/types.ts`, `src/sui/**`, `src/util/**` | `cd keeper && npm run typecheck && npm run build` | ESM, strict TS + `noUncheckedIndexedAccess` + `verbatimModuleSyntax`. `loadConfig(env)` is pure and deep-frozen (10 config tests). |
| T0.4 | App scaffold | **DONE** | `app/package.json`, `vite.config.ts`, `vitest.config.ts`, `src/config.ts`, `src/App.tsx`, `src/routes.tsx`, `src/main.tsx`, `src/components/**`, `src/fixtures/**`, `src/lib/**`, `src/landing/**`, `test/**` | `cd app && npm run build && npm test` | React 19 + Vite 6, four routes (`/` landing, `/deposit`, `/exit`, `/transparency`). Globe textures vendored locally. **The app now has its own vitest suite: 6 files, 84 tests, fully offline** (`test.env` pins every `VITE_*` so a developer's `.env.local` cannot change a result). |
| T0.5 | Hashi adapter + deterministic MOCK | **DONE** | `keeper/src/hashi/{types,limiter,adapter,eventTypes,normalize,mock,index}.ts` | `cd keeper && npm run test -- hashi.mock` | 24 mock tests + 19 limiter tests + 5 cross-parity tests. Logical clock, no `Date.now()`, no `Math.random()`, no I/O. |
| T0.6 | Config wiring proof (G7) | **REGRESSED** | `keeper/.env.example`, `app/.env.example`, `scripts/gates.ps1` | `powershell -File scripts/gates.ps1 ids` → **FAIL** | The invariant held until `scripts/register-deposit.ps1` landed with two hardcoded Hashi ids. See **B11**. `g7`, `g4`, `g2`, `sdk`, `purity`, `transport` all still PASS. |

---

## Phase 1 — Move package (CUT-LINE CRITICAL) — **published on testnet**

| id | Title | Status | Files that carry it | VERIFY | Note |
|---|---|---|---|---|---|
| T1.1 | Vault object + share math | **DONE** | `move/sources/vault.move`, `move/tests/vault_tests.move` | `cd move && sui move test vault` → 42 pass | `Vault<phantom B, phantom Q>` generic over the pair (naming hBTC here would break G7). Share ledger in sats, `mul_div` widens to `u128` before dividing, NAV at DeepBook mid. B4 is closed — the 15 empty-body tests are gone; the file now carries **108 `assert!`s**. |
| T1.2 | `seal_approve` gate | **DONE** | `move/sources/vault.move`, `move/tests/vault_tests.move` | `cd move && sui move test vault` | Identity namespaced to vault id + version epoch; `set_keeper` bumps the epoch, `update_strategy` does not. Rotated-out caps, foreign caps, malformed identity lengths and stale epochs all covered. |
| T1.3 | Gateway: register + composed exit | **DONE** | `move/sources/gateway.move`, `move/tests/gateway_tests.move` | `cd move && sui move test gateway` → 26 pass | `register_exit_address` (20 B P2WPKH / 32 B P2TR, write-once) + `exit_to_bitcoin` composing `hashi::withdraw::request_withdrawal` with the **pinned** address. `g7` gate confirms `hashi::` appears in this file only. |
| T1.4 | Gateway: reclaim + small-exit pool | **DONE** | `move/sources/gateway.move`, `move/tests/gateway_tests.move`, `move/tests/mock_hashi.move` | `cd move && sui move test gateway` | `reclaim_stalled_exit` is **DEPOSITOR-ONLY**; the pooled flush asserts `who == ctx.sender()` (E-M8). `mock_hashi.move` replicates all five upstream asserts. |
| T1.5 | Router: maker + IOC | **DONE** | `move/sources/router.move`, `move/tests/router_tests.move` | `cd move && sui move test router` → 25 pass | Maker leg = `place_limit_order(order_type = 3 /* POST_ONLY */, self_matching_option = 0)` — `place_post_only_limit_order` is **absent from the deployed v20** (E-M6). Granularity gate runs before the book is touched. `try_book_mid` returns `none` on a one-sided book. |
| T1.6 | Gateway unit tests | **DONE** | `move/tests/gateway_tests.move`, `move/tests/mock_hashi.move` | `cd move && sui move test gateway` | 26 tests. Note the structural limit: Move has no DI, so these verify the gateway's *logic* against `mock_hashi`, not the deployed gateway's own call into the real bridge. The linker check at publish is what covers that. |
| — | **Publish to testnet** | **DONE** | `docs/DEPLOYED.md`, `scripts/verify-onchain.mjs` | `node scripts/verify-onchain.mjs` | Package + shared `Vault` + shared `BalanceManager` all resolve live (P1 checks). Not a BUILD-PLAN id; recorded here because everything downstream depends on it. |

---

## Phase 2 — Keeper (CUT-LINE CRITICAL)

Every module under `keeper/src/` is implemented and tested, **and as of this pass the CLI is wired too** — `keeper/src/index.ts` is `@status DONE` with zero `NotImplementedError` left.

| id | Title | Status | Files that carry it | VERIFY | Note |
|---|---|---|---|---|---|
| T2.1 | Real Hashi adapter | **DONE** | `keeper/src/hashi/real.ts` | `cd keeper && npm run test -- hashi.real` → 36 pass | Built from `HashiClient` + raw `moveCall`s + event polling, because most of the API the specs describe does not exist in `0.6.0` (E-K8). Declares an explicit `UnsupportedByUpstream` error for the capabilities the SDK/transport genuinely cannot serve. **The only file in the repo importing `@mysten/hashi`** — the `sdk` gate is now load-bearing (B7 closed). |
| T2.2 | Event watcher | **DONE** | `keeper/src/hashi/watcher.ts` | `cd keeper && npm run test -- watcher` → 22 pass | Reuses `hashi/normalize.ts` for the `WithdrawalSigned` join; deterministic under the mock. |
| T2.3 | `confirm_deposit` crank | **DONE** | `keeper/src/execution/crank.ts`, `keeper/src/index.ts` | `cd keeper && npm run test -- crank` → 59 pass | Permissionless, idempotent, respects the 600 000 ms delay, works on **other people's** deposits. `confirm_deposit` is `entry` ⇒ a PTB command, never a Move `moveCall` (E-M9). This is the live on-camera demo beat. |
| T2.4 | Sponsored deposit sweep | **DONE** | `keeper/src/execution/sweep.ts`, `keeper/src/index.ts` | `cd keeper && npm run test -- sweep` → 23 pass | Sponsorship changes who *pays*, never who the *sender* is. |
| T2.5 | Withdrawal tracker | **DONE** | `keeper/src/execution/exit.ts`, `keeper/src/execution/reclaim.ts`, `keeper/src/index.ts` | `cd keeper && npm run test -- exit` → 45 pass | `exit.ts` builds ONE `gateway::exit_to_bitcoin` moveCall with **no destination argument** (`g2` gate). `reclaim.ts` is an **unsigned PTB builder only** — the `reclaim` command prints a base64 PTB and never signs (E-K7). |
| T2.6 | Strategy: Seal + deterministic evaluate/route | **DONE**, privacy **PARTIAL** | `keeper/src/strategy/{params,serialize,evaluate}.ts`, `keeper/src/privacy/{seal,session,rotation}.ts` | `cd keeper && npm run test -- strategy` → 26 pass; `-- privacy` → 21 pass | `evaluate()` is pure and deterministic (the `purity` gate enforces it); `rulesetHash` is a stable 32-byte digest that covers the helpers. ⚠ `privacy/{seal,session,rotation}.ts` are self-declared **PARTIAL**: every body is real, but the concrete `SealBackend` port is not bound, so nothing has yet encrypted against the live key servers. The published vault's `strategy_ciphertext` is still a one-byte placeholder (`docs/DEPLOYED.md`). |
| T2.7 | Routing: L2 book + maker/IOC split | **DONE** | `keeper/src/routing/{book,deepbook,route,taker}.ts`, `keeper/src/execution/trade.ts` | `cd keeper && npm run test -- routing` → 56 pass | Reads `get_level2_range`, never `mid_price` (which aborts `EEmptyOrderbook` — confirmed live today). The empty book is a **defined** state end to end. |
| T2.8 | Oracle divergence breaker | **DONE** | `keeper/src/oracle/{pyth,twap,divergence,index}.ts` | `cd keeper && npm run test -- oracle` → 49 pass | Pyth **Beta** feed `0xf9c0172b…`, `attributes.symbol === "Crypto.BTC/USD"` exact match, staleness guard. Verified live today: publish_time age **1 s**, BTC/USD 64 092.69. |
| T2.9 | Storage: Walrus put/get | **DONE** | `keeper/src/storage/{walrus,renew}.ts` | `cd keeper && npm run test -- storage` → 24 pass | `WALRUS_EPOCHS` explicit (`< 2` throws in `config.ts` — covered by a config test). Grace window for `certifiedEpoch: null` on fresh blobs (E-M11). |
| T2.10 | End-to-end run loop (mock) | **DONE** | `keeper/src/index.ts`, `keeper/test/e2e.mock.test.ts` | `cd keeper && npm run test -- e2e.mock` → **13 pass** | Landed during this pass. Zero TODOs, zero `NotImplementedError`. The e2e test **asserts** the properties rather than assuming them: *"the loop reads NO wall clock and opens NO socket"*, and `runLoop` honours `--once`/`--ticks`. **B5 and B12 are closed.** |

---

## Phase 3 — App (CUT-LINE CRITICAL)

| id | Title | Status | Files that carry it | VERIFY | Note |
|---|---|---|---|---|---|
| T3.1 | Deposit screen | **DONE** | `app/src/screens/deposit/{DepositScreen.tsx,useDepositAddress.ts}`, `app/src/hashi/{index,depositAddress}.ts`, `app/src/components/QrCode.tsx`, `app/test/depositAddress.test.ts`, `app/test/hashiAdapter.test.ts` | `cd app && npm run build && npm test` | Client-side Taproot derivation; `generateDepositAddress` takes **four** args and a 32-byte `Uint8Array` (E-K10). `app/src/hashi/depositAddress.ts` is the app's single Hashi boundary (G7). Now covered by the app suite (17 adapter tests). |
| T3.2 | Exit screen | **STUB (form only)** · everything else DONE | `app/src/screens/exit/ExitScreen.tsx` | `cd app && npm run build` | **3 of the 4 remaining TODOs in the whole repo.** `ExitRequestForm` renders a placeholder instead of a controlled bigint sats input, and the pinned address comes from a fixture rather than an on-chain read. Everything around it is real and DONE: `RegisterExitAddress.tsx`, `ptb.ts`, `vaultRead.ts`, `model.ts`, `CallPreview.tsx`, `ExitTimeline.tsx`, `lib/bech32.ts` (+ `app/test/bech32.test.ts`), `PinningExplainer`. |
| T3.3 | zkLogin + sponsored path | **DONE (unverified end-to-end)** | `app/src/session/{enoki.ts,useSession.ts}`, `app/src/components/ZkLoginButton.tsx`, `app/test/enoki.test.ts`, `scripts/check-enoki.mjs` | `node scripts/check-enoki.mjs <origin>`; manual: login → deposit with zero user SUI | Enoki is configured, the config check passes, and `app/test/enoki.test.ts` covers the wiring. **No browser login was run in this pass**, so the "zero user SUI" claim is not something anyone has personally observed end-to-end. Treat as DONE-pending-manual. Note the file is `session/enoki.ts`, not the `session/zkLogin.ts` the BUILD-PLAN names. |

---

## ================= CUT LINE =================

```
cd move   && sui move build && sui move test            # PASS — 139/139, bodies are real
cd keeper && npm run build && npm run test -- e2e.mock  # PASS — 13 tests
cd app    && npm run build                              # PASS
```

**The cut line is met.** All three legs are green with real coverage, and the named acceptance test now exists and asserts determinism and offline-ness rather than assuming them. What remains is not cut-line work: the exit-screen form (T3.2), the landing stats (T5.2), the `ids` gate regression (**B11**), and the empty book (**B2** — an ops blocker, not a code one).

## ============================================

---

## Phase 4 — Envelope + journal + verify

| id | Title | Status | Files that carry it | VERIFY | Note |
|---|---|---|---|---|---|
| T4.1 | Envelope: redemption buffer | **DONE** | `move/sources/envelope.move`, `move/tests/envelope_tests.move` | `cd move && sui move test envelope` → 34 pass | The **G5 Move twin** of `keeper/src/hashi/limiter.ts`: R9 golden vectors v1–v7 are asserted on both sides. `deployable_sats` subtracts the pending-exit earmark **before** the buffer; `check_action` fails closed on a missing oracle price. Saturating helpers never abort. `envelope` is the intra-package leaf and takes primitives, never `&Vault`. |
| T4.2 | Journal: decision records | **DONE** | `keeper/src/journal/{schema,record}.ts`, `move/sources/journal.move`, `move/tests/journal_tests.move` | `cd keeper && npm run test -- journal` → 30 pass; `cd move && sui move test journal` → 12 pass | Decision records carry the limiter reading, peg-flow, oracle, book and ruleset hash; blob ids are emitted on chain with a monotonic-seq guard. |
| T4.3 | Verify: trustless limiter replay | **DONE** | `keeper/src/verify/{index,limiter,replay}.ts`, `keeper/src/index.ts` | `cd keeper && npm run test -- verify` → 34 pass; `-- limiter.cross` → 5 pass | **B6 is closed**: `keeper/test/limiter.cross.test.ts` now imports the real `../src/verify/limiter.js`; the duplicate is gone, so the G5 parity assertion cannot drift. `deriveLimiter` uses only on-chain-observable events + the two genesis scalars. |

---

## Phase 5 — Stretch

| id | Title | Status | Files that carry it | VERIFY | Note |
|---|---|---|---|---|---|
| T5.1 | Peg-flow signal | **DONE** | `keeper/src/strategy/pegflow.ts` | `cd keeper && npm run test -- strategy.pegflow` → 15 pass | Signal is public and derived from `DepositApproved`/`WithdrawalRequested`; the response (the lean) is the secret, clamped to ±`spreadBps` so peg-flow can never invert a quote. Utilisation is measured against the genesis cap, with an honest fallback bound when it is unknown. |
| T5.2 | Transparency panel | **PARTIAL** | `app/src/screens/transparency/**`, `app/src/components/BridgeColumn.tsx`, `app/src/landing/stats.js` | `cd app && npm run build` | The screen itself is DONE — `DecisionLogViewer`, `EncryptedStrategyPanel`, `verifyClient.ts`, `walrus.ts`, `display.ts`, `BridgeColumn`, `StaleBanner`, `TrustModelDisclosure`, `KeeperCapabilityBadge` all real. **1 remaining TODO in `app/src/landing/stats.js`**: the landing page's headline stats are still placeholders. No congestion copy anywhere (E-A6). |
| T5.3 | Deposit-ticket TTO | **NOT-STARTED** | — | `cd move && sui move test ticket` | No file exists. Derivation-unblocked (U4 = YES) but **mint-unproven**; out of scope for the demo. |

### Beyond the BUILD-PLAN — ops tooling that carries no task id

| What | Status | Files | Note |
|---|---|---|---|
| Testnet publish | **DONE** | `docs/DEPLOYED.md` | Package, shared `Vault`, shared `BalanceManager`, caps, digests. Verified live by `verify-onchain` P1. |
| Signet UTXO registration | **DONE** | `scripts/register-deposit.ps1` | Reverses the txid for you (R14.2) and refuses below 6 confirmations (R14.3). ⚠ trips the `ids` gate — **B11**. |
| Book seeding | **DONE (reports honestly; cannot fix B2)** | `scripts/seed-book.mjs`, `scripts/seed-book.ps1` | Reads live pool params by devInspect, prints what is *obtainable* with evidence, refuses any write without `--execute`, and refuses a bid deviating from the Pyth Beta feed beyond `--max-deviation-bps`. Established that the DBUSDC `TreasuryCap` is `AddressOwner`, not shared ⇒ we cannot mint the quote asset either. |
| Enoki origin check | **DONE** | `scripts/check-enoki.mjs` | Verifies a deployed origin is registered so zkLogin does not 403. |

---

## Environment

| | |
|---|---|
| `sui` CLI | **1.76.0-6effb4523834** at `%LOCALAPPDATA%\sui\sui.exe`. ⚠ Not reliably on `PATH` in agent shells — prepend it: `$env:PATH = "$env:LOCALAPPDATA\sui;$env:PATH"`. ⚠ The test filter is **positional** (`sui move test vault`); `--filter` is not a flag in 1.76.0. |
| `sui client active-env` | `testnet`, chain id `4c78adac` (asserted by `verify-onchain` P0) |
| node / npm | 24.13.0 / 11.6.2 |
| **Deployer / keeper address** | **`0xd41b0cd83fc1a497a5899eb686e2c7561e75e6d62db2270860d72542f63f333d`** — also the active address. Gas: 3 coins, **~21.85 SUI** total. |
| **`aphotic` package** | **`0xbe433a2726fc61391d180ce55cdb8177f9647760b23a7704d42e3b5b9bb72d66`** (v1, Immutable) — modules `envelope`, `gateway`, `journal`, `router`, `vault` |
| **`Vault<hBTC, DBUSDC>`** | **`0xf03832c92d4bf745ac720c52fe9198fc928028ce51991059bfe59c0e4ef374e8`** — Shared, `initialSharedVersion = 947353676` |
| **DeepBook `BalanceManager`** | **`0x5766ed0b5e3fd310da9ccd723912198450872d9e2c83a473ed59cd5ab51990e2`** — Shared, `initialSharedVersion = 947353675` |
| `VaultCap` | `0x232ed471922011b4083e20bfb7c58f923f8c3fd4691ca5522364ff3d506d9b3d` — owned by the deployer |
| `UpgradeCap` | `0xf2660f561f358e02d88e82ec4792916bdd1193f8553cdd6ab43e81c96fd72ca9` — owned by the deployer |
| Envelope params at creation | `max_slippage_bps 50` · `max_notional_per_epoch_sats 100_000_000` · `min_cooldown_ms 30_000` · `buffer_ratio_bps 2_000` · `limiter_refill_rate 115_740` · `limiter_max_capacity 10_000_000_000` · `epoch_start_ms 1784984759062`. Full receipts: `docs/DEPLOYED.md`. |
| Live Hashi config (re-verified today) | withdrawal min `30_000` · deposit min `30_000` · deposit delay `600_000` ms · confirmation threshold `6` · cancel cooldown `3_600_000` ms · `paused = false` |
| hBTC supply on testnet | `22 603 929 919` sats (226.039 BTC) — none of it ours yet |
| Secret handling | The private key lives **only** in the OS Sui keystore and `keeper/.env` (gitignored). Never in `.env.example`, any doc, or a commit. |
| **RPC transport policy** | `https://fullnode.testnet.sui.io:443` serves **gRPC v2 only — JSON-RPC returns HTTP 404**. Default client is `SuiGrpcClient` (`@mysten/sui/grpc`), constructed in **exactly one place per package**: `keeper/src/sui/client.ts`, `app/src/lib/suiClient.ts`. The `transport` gate enforces it. `SuiJsonRpcClient` against the mirror `https://rpc-testnet.suiscan.xyz:443` is for probes only — `verify-onchain.mjs` uses it. |
| Guardian transport | `GET {guardian_url}/info` is **HTTP/2 only** (HTTP/1.1 → 464). Use `node:http2`; global `fetch`, `curl` and the SDK's `fetchGuardianInfo` all fail. |
| Windows gotcha | Never rewrite a `.move` file with PowerShell `Set-Content -Encoding utf8` — PS 5.1 writes a UTF-8 BOM and the compiler rejects it (`E01001`). Use `[System.IO.File]::WriteAllText` with `New-Object System.Text.UTF8Encoding($false)`. |

---

## Known blockers

| # | Blocker | Severity | Status | Detail |
|---|---|---|---|---|
| **B2** | **hBTC/DBUSDC book is empty on both sides** | **HIGH — functional** | **OPEN** (tooling landed) | Re-confirmed live today: `pool::mid_price` aborts `MoveAbort 2 = EEmptyOrderbook`, `pool::get_level2_range` returns **0 bid levels**. `treasury::mint` is `public(package)` ⇒ **we cannot mint hBTC ourselves**, and `scripts/seed-book.mjs` established a second wall: the **DBUSDC `TreasuryCap` is `AddressOwner`, not shared**, so `DBUSDC::mint` is unreachable for us too (same for DBUSDT and DBTC). `seed-book.mjs` now reports honestly what *is* obtainable from live reads and refuses any write without `--execute`, but it cannot conjure inventory. Note `mid_price` asserts **both** sides — seeding one side does not make it callable. Every consumer handles the empty book as a *defined* state (`try_book_mid` → `none`, `evaluate` → `noop cause=no-mid`), so nothing crashes; there is simply no price to show. **This is an ops/inventory problem, not a code one.** |
| **B11** | `ids` gate FAILS — two hardcoded Hashi ids in `scripts/register-deposit.ps1` | **MEDIUM — invariant** | **OPEN** | Lines 48–49 inline `$HASHI_PKG` and `$HASHI_OBJ`. G7 says canonical ids live only in `keeper/src/config.ts`, `app/src/config.ts`, the `.env.example` files and `move/Move.toml`. Either source them from `keeper/.env` (which is what `seed-book.mjs` does — it passes the gate) or add an explicit, documented exemption for operator scripts. Until then `verify-all.ps1` reports `7 PASS · 1 FAIL` and **we cannot claim a clean master gate**. |
| ~~B5~~ | ~~`npm run test -- e2e.mock` matches no test file~~ | — | **RESOLVED** | `keeper/test/e2e.mock.test.ts` landed with 13 tests, including *"the loop reads NO wall clock and opens NO socket"*. |
| ~~B12~~ | ~~Every keeper CLI handler throws `NotImplementedError`~~ | — | **RESOLVED** | `keeper/src/index.ts` is `@status DONE`; zero `NotImplementedError` remain. `crank`, `sweep`, `exit`, `reclaim`, `verify`, `run`, `create-vault` all dispatch to their (tested) modules. |
| **B13** | Seal encryption is not bound to a live backend | **LOW — narrative** | **OPEN (new)** | `keeper/src/privacy/{seal,session,rotation}.ts` are self-declared PARTIAL: the logic is real and tested, the concrete `SealBackend` port is not wired. The published vault's `strategy_ciphertext` and `strategy_blob_id` are one-byte placeholders. The "encrypted strategy" claim is therefore **architecturally true and not yet demonstrated on chain** — say it that way. |
| ~~B1~~ | ~~Signet faucet drip never started~~ | — | **RESOLVED** | Sats received. Tx `2275d89012c7e3a408a5c04fcb8203f0d1b0702992713e6dd36197ad99ddeac9`, block 314710, vouts 238 and 248, 144 137 sats each. ⚠ The first batch `04cb601a…` was **RBF-replaced** and now 404s on both explorers — see RECON R14.3. Registration is gated on 6 confirmations; at last check we were at 5. |
| ~~B3~~ | ~~`Move.toml` cannot declare `[environments]`~~ | — | **RESOLVED** | Block deliberately absent, documented in the file header. `sui move build` is green. |
| ~~B4~~ | ~~15 empty-body tests in `vault_tests.move`~~ | — | **RESOLVED** | 42 vault tests, 108 `assert!`s. |
| ~~B6~~ | ~~`deriveLimiter` duplicated in a test file~~ | — | **RESOLVED** | `keeper/test/limiter.cross.test.ts` imports `../src/verify/limiter.js`; the file's own banner records the closure. |
| ~~B7~~ | ~~Nothing imports `@mysten/hashi` ⇒ the `sdk` gate passes vacuously~~ | — | **RESOLVED** | `keeper/src/hashi/real.ts` imports it for real; the gate is load-bearing. |
| ~~B9~~ | ~~`#[allow(unused_const, unused_field)]` suppressions~~ | — | **RESOLVED** | No `allow(unused` remains anywhere under `move/sources/`. |
| ~~B10~~ | ~~Limiter genesis scalars off by ~100×~~ | — | **RESOLVED** | `refill_rate = 115_740`, `max_bucket_capacity = 10_000_000_000` are the live values and are what the vault was created with. |
| **B8** | `move/Move.lock` records Windows backslash subdir paths | LOW — portability | **OPEN** | e.g. `subdir = 'crates\sui-framework\packages\move-stdlib'`. Machine-generated; may need regeneration on Linux/macOS CI. Harmless on this machine. |

### Docs that still need a correction pass (not blocking code)

- `docs/RECON.md` **R9 golden vectors #1 and #7** print `105_000`; the correct value is **`100_150`**. Both twins encode `100_150` and cross-test green — only the doc is wrong. The test names now carry the erratum inline.
- `docs/RECON.md` **R3** states the framework rev is `22f9fc97…`; the resolver pins `d50b7888…` (the CLI's system environment supplies the framework). Harmless, misleading.
- `docs/FACTS.md#events` still lists `utxo_pool::UtxoSpent`, which is **absent** from the verified R8 inventory and is not modelled anywhere.
- `docs/BUILD-PLAN.md`'s canonical-identifier table still lists the **superseded v17** DeepBook package and the **stable-channel** Pyth feed. Use v20 `0xd874d241…` and Beta `0xf9c0172b…`.
- `docs/BUILD-PLAN.md` T1.6 points at `move/sources/tests/gateway_tests.move`; tests actually live at `move/tests/` (E-M2 — otherwise the `g7` gate is unpassable).
- `docs/BUILD-PLAN.md` T3.3 names `app/src/session/zkLogin.ts`; the file is `app/src/session/enoki.ts`.

---

## Run log — 2026-07-25 16:26–16:31, verbatim

```
$ cd move && sui move test
Test result: OK. Total tests: 139; passed: 139; failed: 0
  envelope_tests 34 · gateway_tests 26 · journal_tests 12 · router_tests 25 · vault_tests 42

$ cd keeper && npx vitest run          # 16:26
 Test Files  19 passed (19)
      Tests  476 passed (476)

$ cd keeper && npx vitest run          # 16:36, after another agent's commit
 Test Files  19 passed (19)
      Tests  481 passed (481)
   Duration  8.42s

$ cd keeper && npm run test -- e2e.mock
 ✓ test/e2e.mock.test.ts > the loop reads NO wall clock and opens NO socket
 ✓ test/e2e.mock.test.ts > runLoop drives the same tick function and honours --once / --ticks
 Test Files  1 passed (1)
      Tests  13 passed (13)

$ cd app && npm test
 ✓ test/hashiAdapter.test.ts (17 tests)
 ✓ test/components.test.tsx (6 tests)
 Test Files  6 passed (6)
      Tests  84 passed (84)

$ cd app && npm run build
tsc --noEmit && vite build
✓ built in 18.54s

$ node scripts/verify-onchain.mjs
  28 PASS · 0 FAIL · 0 WARN · 7 INFO
  P3 pool::mid_price          PASS  MoveAbort 2 = EEmptyOrderbook (known empty book)
  P3 pool::get_level2_range   PASS  bids: 0 level(s) (empty book)
  P6 pyth price freshness     PASS  BTC/USD 64088.50 · age 0s

$ powershell -NoProfile -File scripts/gates.ps1
  [PASS] g7  [PASS] g4  [PASS] g2  [FAIL] ids  [PASS] sdk  [PASS] purity  [PASS] transport
  [INFO] todo  4 marker(s) across 2 task id(s)   TODO(T3.2) 3 · TODO(T5.2) 1
  6 PASS · 1 FAIL · 0 SKIP

$ powershell -NoProfile -File scripts/gates.ps1 ids
  [FAIL] ids   Canonical ids only in config.ts / .env* / Move.toml
    scripts/register-deposit.ps1:48: $HASHI_PKG = '0xfcea10ca…f4360'
    scripts/register-deposit.ps1:49: $HASHI_OBJ = '0x22c0ce66…528f8'

$ powershell -NoProfile -File scripts/verify-all.ps1
  STEP               RESULT SECONDS  NOTE
  move build         PASS   7,9      exit 0
  move test          PASS   25,8     exit 0
  keeper typecheck   PASS   7,2      exit 0
  keeper build       PASS   4,8      exit 0
  keeper test        PASS   13,6     exit 0
  app build          PASS   28,9     exit 0
  gates              FAIL   8        exit 1
  verify-onchain     PASS   4,5      exit 0
  7 PASS · 1 FAIL · 0 SKIP
```

⚠ `scripts/verify-all.ps1` does **not** yet run `cd app && npm test` — the app suite is new. Add it as a ninth step.
